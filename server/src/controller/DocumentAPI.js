/**
 * Signed documents can be uploaded to this API by users of this application.
 * These documents can then be distributed and authenticated against this platform.
 * 
 * Users do not then need to manage everyone's public keys. They can simply upload the document
 * and determine if it's authentic and comes from the person that they expect.
 */
class DocumentAPI {

    upload(buffer, signature){

    }

    isAuthentic(buffer, signature, signingUser){

    }
}